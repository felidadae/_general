# Look at install.sh file.

# Source install.sh to use function 
#	addDotfile to add new dot file which should
#	placed in directly in ~/

# File which should be placed directly in ~/
# should be placed in:
# ./__user_dir_level__ 
